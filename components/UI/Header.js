import { NavBar } from "./NavBar";
import SearchInput from "./SearchInput";

const Header = (props) => {
  return (
    <>
      <SearchInput />
      <NavBar />
    </>
  );
};

export default Header;
